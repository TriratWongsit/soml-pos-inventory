// ---------------------------------------------------------------------
// เตรียมฐานข้อมูลทดสอบก่อนเริ่มชุดทดสอบการทำงานร่วมกัน
//
// ใช้ฐานข้อมูล soml_test แยกจากฐานข้อมูลใช้งานจริง เพื่อให้รันทดสอบซ้ำได้
// โดยไม่กระทบข้อมูลที่ใช้เก็บภาพหลักฐาน และผลทดสอบเหมือนเดิมทุกครั้ง
// ---------------------------------------------------------------------
const path = require('path');
const { execFileSync } = require('child_process');

module.exports = async () => {
  const root = path.join(__dirname, '..', '..', '..');
  execFileSync('bash', [path.join(root, 'scripts', 'reset-db.sh'), 'test'], {
    cwd: root,
    stdio: 'pipe',
  });
};
